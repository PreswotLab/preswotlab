extends default.pug

block content 
    #login-form
        div 
            form(method="POST")
                select(name="dbkind")
                    option(value="MYSQL") MYSQL
                    option(value="MARIADB") MARIADB
                    option(value="MSSQL") MSSQL
                input(type="text" name='dbhostip' required placeholder="Database Host/Ip")
                input(type="text" name="dbport" required placeholder="Database Port")
                input(type="text" name="dbusername" required placeholder="Username")
                input(type="password" name="dbpassword" required placeholder="User password")
                input(type="text" name="dbname" required placeholder="Database name ")
                input(type="submit" value="로그인")
                br
