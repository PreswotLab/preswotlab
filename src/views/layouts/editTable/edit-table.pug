extends ../default
include ../../mixins/editTable/tbNameScanYnEdit.pug

block content
	div.table-grid
		each element in tbNameScanYn
			+tbNameScanYnEdit(element)
		else
			span.notables 테이블이 없습니다.

block scripts
